name: Payments API CI

on:
  push:
    branches: ["**"] # Dispara em qualquer branch
    paths:
      - "src/services/AcademyIO.Payments.API/**"
      - "src/building-blocks/**"
      - ".github/workflows/payments-ci.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "src/services/AcademyIO.Payments.API/**"
      - "src/building-blocks/**"
      - ".github/workflows/payments-ci.yml"

jobs:
  ci:
    uses: ./.github/workflows/ci-template.yml
    with:
      project_name: "src/services/AcademyIO.Payments.API/"
      dockerfile_path: "./src/services/AcademyIO.Payments.API/Dockerfile"
      docker_image_name: "academyio-payments-api"
    secrets: inherit
