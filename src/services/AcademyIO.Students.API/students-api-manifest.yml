# --- INSTRUÇÕES PARA CRIAÇÃO DE SECRETS ---
# Os segredos reais NÃO devem ser versionados. Crie-os manualmente no cluster:
#
# kubectl create secret generic students-api-secret -n academyio \
#   --from-literal=ConnectionStrings__DefaultConnection='Server=sqlserver-service;Database=StudentsDb;User Id=sa;Password=<YourStrong!Passw0rd>;TrustServerCertificate=true'
# ------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  name: students-api-deployment
  namespace: academyio
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: students-api
  template:
    metadata:
      labels:
        app: students-api
    spec:
      containers:
        - name: students-api
          image: luisfelipebcc/academyio-students-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: students-api-config
            - secretRef:
                name: students-api-secret
          livenessProbe:
            httpGet:
              path: /health/live
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: students-api-service
  namespace: academyio
spec:
  selector:
    app: students-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: students-api-config
  namespace: academyio
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  DOTNET_RUNNING_IN_CONTAINER: "true"
  ASPNETCORE_URLS: "http://*:80"
  MessageBus__ConnectionString: "amqp://guest:guest@rabbitmq-service:5672/"
